# TODO: Archivos a ignorar en Git - Player 3 (Fullstack)
# Paso 1: Ignorar node_modules, dist, .env, builds
# Paso 2: Ignorar logs, cache, IDE files
# Entregable: git status solo muestra archivos de código
node_modules/
dist/
lib/
.build/
.env
.env.local
*.log
.DS_Store
.idea/
.vscode/
coverage/

# Config de cliente Firebase para apps nativas: pertenece al proyecto/ambiente
# de cada quien (dev/staging/prod), no se versiona. Descargar con:
#   firebase apps:sdkconfig ANDROID <APP_ID> --project <PROJECT_ID>
google-services.json
GoogleService-Info.plist
