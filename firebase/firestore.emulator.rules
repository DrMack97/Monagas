rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: Modo desarrollo - más permisivo
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
