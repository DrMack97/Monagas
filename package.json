{
  // TODO: Scripts globales del monorepo - Player 3 (Fullstack)
  // Paso 1: Definir scripts para dev, build, test, lint
  // Paso 2: Definir packageManager: "pnpm@8.x"
  // Entregable: pnpm dev, pnpm build, pnpm test funcionan
  "name": "monagas",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "firebase:emulate": "firebase emulators:start"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "prettier": "^3.2.0",
    "eslint": "^8.57.0"
  }
}
