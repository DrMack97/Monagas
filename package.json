{
  "name": "monagas",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "firebase:emulate": "cd firebase && cross-env FUNCTIONS_DISCOVERY_TIMEOUT=60 npx firebase emulators:start --project well-testing-dev"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "prettier": "^3.2.0",
    "eslint": "^8.57.0",
    "cross-env": "^7.0.3",
    "firebase-tools": "^15.22.4"
  }
}
